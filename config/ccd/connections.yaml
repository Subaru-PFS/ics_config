default: &DEFAULT
  listen:
    interface: bee-$cam.pfs     # localhost is safest, if on same host
                                # as tron hub.
    port: 9103
  fee:
    port: /dev/ttyS1

  controllers: &CONTROLLERS
    all: [fee, ccd]
    starting: [fee, ccd]

$cam:
  <<: *DEFAULT
  
